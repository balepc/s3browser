<div class="folder_container">
  <table width="100%">
    <% @buckets.each do |bucket| %>
      <tr>
        <td width="17px"><%= link_to image_tag('folder.png'), "/buckets/#{bucket.name}" %></td>
        <td><%= link_to bucket.name, "/buckets/#{bucket.name}" %></td>
      </tr>
    <% end %>
  </table>
</div>